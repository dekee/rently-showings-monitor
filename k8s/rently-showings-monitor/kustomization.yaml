apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # Create your own secret (do not commit real credentials). Use secret-template.yaml as a starting point.
  # - secret-template.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml


